[build-system]
requires = ["setuptools>=61","wheel"]
build-backend="setuptools.build_meta"

[project]
name="oqubit"
version="0.1.0"
dependencies=["numpy>=2.0"]
requires-python = ">=3.9"
readme = "README.md"